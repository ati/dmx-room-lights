<%= form_for(@color_saturation) do |f| %>
  <% if @color_saturation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@color_saturation.errors.count, "error") %> prohibited this color_saturation from being saved:</h2>

      <ul>
      <% @color_saturation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :color_group %>:
    <%= f.collection_select :color_group_id, ColorGroup.all, :id, :displayname %><br />
  </div>
  <div class="field">
    <%= f.label :distance %> <small>(0..1)</small>:
    <%= f.text_field :distance, :size => 8 %><br />
  </div>
  <div class="field">
    <%= f.label 'Saturation' %> <small>(0..100)</small>:
    <%= f.number_field :s, :size => 8 %><br />
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
