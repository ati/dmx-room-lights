<%= form_for(@fixture) do |f| %>
  <% if @fixture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fixture.errors.count, "error") %> prohibited this fixture from being saved:</h2>

      <ul>
      <% @fixture.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :galaxy %>:
    <%= f.collection_select :galaxy_id, Galaxy.all, :id, :displayname %><br />
  </div>
  <div class="field">
    <%= f.label :distance %> <small>(0..1)</small>:
    <%= f.text_field :distance, :size => 8 %><br />
  </div>
  <div class="field">
    <%= f.label :start_address %> <small>(0..512)</small>:
    <%= f.text_field :start_address, :size => 8 %><br />
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
